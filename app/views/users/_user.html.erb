<tr id="<%= dom_id user %>" class="UserRow">
  <td class="UserRow__name">
    <%= user.name %>
  </td>

  <td class="UserRow__email">
    <%= user.email %>
  </td>

  <td class="UserRow__teams">
    <ul class="fr-tags-group">
      <% user.teams.each do |team| %>
        <li><%= link_to team.name, edit_team_path(team), class: "fr-tag fr-tag--sm UserRow__teamTag" %></li>
      <% end %>
    </ul>
  </td>

  <td class="UserRow__roles">
    <%= tag.span user.role, class: "fr-tag fr-tag--sm UserRow__roleTag" %>
  </td>

  <td class="UserRow__actions">
    <ul class="UserRow__actionsGroup cm-btns-group cm-btns-group--sm cm-btns-group--inline-sm">
      <% if can? :destroy, user %>
        <li><%= button_to t(".delete_user"), team_user_path(@team, user.id), method: :delete, data: { turbo_confirm: t(".delete_user_confirm") }, class: "cm-btn cm-btn--sm cm-btn--secondary fr-icon-delete-bin-line" %></li>
      <% end %>

      <li><%= link_to t(".edit_user"), edit_team_user_path(@team, user.id), class: "cm-btn cm-btn--sm cm-btn--primary fr-icon-pencil-line" %></li>
    </ul>
  </td>
</tr>
