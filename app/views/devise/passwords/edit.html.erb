<%= content_for :back do %>
  <%= link_to t(:back), root_path, class: "cm-btn cm-btn--icon-left cm-icon-arrow-left" %>
<% end %>
<%= render partial: "main_menu" %>
<div class="Page Page__change-password">
  <div class="Block Block__change-password">
    <h1><%= t(".change_your_password") %></h1>
    
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
    
      <fieldset class="cm-fieldset">
        <%= render "devise/shared/error_messages", resource: resource %>
        <%= f.hidden_field :reset_password_token %>
      
        <div class="cm-fieldset__element cm-input-group">
          <%= f.label :password, t(".new_password"), class: "cm-label" %>
          <% if @minimum_password_length %>
            <span class="cm-hint-text">(<%= @minimum_password_length %> <%= t("devise.shared.forms.min_char") %>)</span>
          <% end %>
          <%= f.password_field :password, autocomplete: "new-password", class: "cm-input cm-password__input", aria_required: true %>
        </div>
      
        <div class="cm-fieldset__element cm-input-group">
          <%= f.label :password_confirmation, class: "cm-label" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "cm-input cm-password__input", aria_required: true %>
        </div>
      </fieldset>
    
      <fieldset class="cm-fieldset">
        <div class="cm-fieldset__element cm-btns-group">
          <%= f.submit t(".change_my_password"), class: "cm-btn" %>
        </div>
      </fieldset>
    <% end %>
    
    <%= render "devise/shared/links" %>
  </div>
</div>