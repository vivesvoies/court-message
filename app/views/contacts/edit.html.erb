<div class="Page Page__edit-contact">
  <div class="Block Block__edit-contact">
    <h1><%= t(".edit_contact") %></h1>

    <%= render "form", contact: @contact %>

    <div>
      <%= link_to "Show this contact", @contact %> |
      <%= link_to "Back to contacts", contacts_path %>
    </div>
  </div>
</div>