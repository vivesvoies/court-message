  <div class="NavMenu">
    <h1 class="Menu__Title">
      <%= t(:site_title) %>
    </h1>
    <%= button_to t(:sign_out), destroy_user_session_path, method: :delete, class: "cm-btn cm-btn--tertiary-no-outline cm-btn--icon-left cm-icon-logout", form: { class: "NavMenu__signout" },  data: { turbo_frame: "_top" } if signed_in? %>
  </div>
