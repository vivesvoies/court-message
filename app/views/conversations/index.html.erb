<div class="Viewer" id="viewer" data-controller="viewer">
  <div id="conversation_sidebar" class="ConversationSidebar">
    <%= turbo_frame_tag "conversation_header", class: "ConversationHeader" do %>
      <%= render partial: "conversations/header", locals: { team: @team } %>
    <% end %>
    
    <%= turbo_stream_from "team_conversations_list_#{@team.id}" %>
    <ol id="conversations" class="ConversationList" data-controller="conversation-list" data-conversation-list-item-class="Conversation" data-conversation-list-active-class="Conversation--active" data-conversation-list-unread-class="Conversation--unread">
      <%= render @conversations %>
    </ol>
  </div>

  <%= turbo_frame_tag "conversation_detail", class: "ConversationDetail", data: { controller: "conversation-detail" } do %>
    <%= render partial: "conversation_detail", locals: { conversation: @conversation } %>
  <% end %>

  <%= turbo_frame_tag "conversation_userbar", class: "ConversationUserbar" do %>
    <%= render partial: "conversation_userbar" %>
  <% end %>

  <div class="TabBar">
    <%= render partial: "tab_bar" %>
  </div>
</div>
